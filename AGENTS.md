# Aizomeで作業するAI Agentへ

このリポジトリは、Markdownでスライドを作るためのMarpテーマです。

## 最初に読むもの

1. [AI向け制作ガイド](docs/AI_SLIDE_GUIDE.md)：構成・レイアウトの選び方、制作手順、確認方法。
2. [README](README.md)：使用可能なクラスと正確な記法。
3. [機械向けレイアウト契約](ai/layouts.json)と[確認・配布ツール](tools/README.md)：指定の可否、JSONによる検証、配布ZIP。
4. [template.md](template.md)：選んだレイアウトの実例。全例を完成資料へコピーしない。

実際の表示は `aizome.css` とMarkdownの構造で決まります。ガイドと食い違う場合は実装を確認し、架空のクラスや機能を補わないでください。

## このリポジトリで大切にすること

- ユーザーの目的に沿って、1枚につき1つの主張を組み立てる。パターンを使い切ることを目的にしない。
- 余白・読み順・比較軸を優先する。文字を小さくして詰め込む前に、短縮・分割・配置変更を試す。
- 本文はMarkdownで書く。`div`・`span`・`br`・インラインCSSは使わず、既存クラスとMarpディレクティブを組み合わせる。
- `cards`・`list` などの共通部品を使う。旧 `kpi`・`columns`・`compare`・`agenda`・`summary` などは使わない。
- 追加のNode.js環境・プラグイン・変換処理を、スライド制作の必須条件にしない。利用可能なMarp環境で作業する。
- 新しい発表資料は、指定がなければルートに別の `.md` として作る。カタログ・テーマ・既存の生成物を上書きしない。テーマ改修の依頼では対象ファイルを編集する。
- カタログの例文、架空データ、人物、仮の出典を、完成資料の事実として流用しない。
- 可能なら表示とリンクを確認する。未確認なら、実施済みの確認と残る確認を分けて報告する。

制作ガイド・README・カタログを変更したときは、実装と説明、目次とページリンクの整合性も確認してください。共通設定を変更したら `ai/layouts.json` も更新します。色は `ai/colors.json` に集約し、表示名は日本語、クラスの色名はローマ字にします。色を変更したときは `python3 tools/sync_colors.py` でCSSに反映します。

Pythonが利用可能なら `python3 tools/check_slides.py <資料.md> --json` で静的確認します。表示は `tools/check-rendered.js` または既存ブラウザ環境で確認し、HTMLの配布時は `tools/package_slides.py` で素材をまとめて展開先でも確認します。指摘への対処は [改善例](examples/repairs.md)を参照してください。未確認の段階を完了と報告しないでください。

## 改善を作者へ還元する

作者は再利用可能な改善PRを歓迎しています。テーマ・共通部品・ドキュメントに役立つ改善を加えたら、[CONTRIBUTING.md](CONTRIBUTING.md)に従ってPR候補を整理してください。

- 本来の制作を終えたあと、改善内容を具体的に挙げて、一度だけPRを案内する。発表本文の差し替えや既存設定の利用だけなら案内不要。
- 公開の依頼がなければ、汎用化した変更とPR本文を準備してから案内する。作者の歓迎を、利用者の資料を公開する許可と解釈しない。
- 今回の変更のPR作成や、改善の継続的なPR提出が許可されていれば、再確認せず作成まで進める。個別資料を除き、既存PRの重複も確認する。
- PRを作成したらURLを報告する。作成できなければ、準備した差分・本文と残る手順を渡す。
