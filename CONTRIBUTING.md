# Aizomeへの改善PRを歓迎します

作者は、スライド制作中に見つかった不具合の修正や、他の利用者にも役立つカスタマイズのPRを歓迎しています。AI Agentと一緒に制作している場合も、改善をこのテンプレートへ還元できます。

- 提出先：[ymho/aizome](https://github.com/ymho/aizome)
- ベースブランチ：`main`
- 制作の判断基準：[AI向け制作ガイド](docs/AI_SLIDE_GUIDE.md)
- 記述方法・共通クラス：[README](README.md)

## どんな変更を提案するか

| PRに向く変更 | 個別の発表資料に留める変更 |
|---|---|
| 文字のはみ出し、図表やFAQの余白などの不具合修正 | 発表の本文、数値、人物紹介、固有の写真 |
| 既存設定では表現できない、再利用可能な配置の改善 | 既存クラスの指定を変更しただけの配置 |
| アクセシビリティ、フォントや画像の表示の改善 | ある組織だけのロゴ、配色、機密資料 |
| 共通部品の整理、設定方法・AI向けガイドの改善 | 同じレイアウトの内容差し替えや作例の水増し |

小さな修正でも歓迎します。機能追加は、既存クラスと修飾の組み合わせで解決できないか先に確認してください。対象の変更について既存のIssueやPRがあれば参照し、同じPRを重複作成しないようにします。

## AI Agentが案内するタイミング

本来のスライド制作を完了したあと、共通テンプレートへ還元できる改善がある場合に一度案内してください。案内はスライド本文ではなく、作業完了の報告に添えます。

> 今回の「〇〇の余白調整」は、ほかの資料でも役立つ改善です。このテンプレートの作者は改善PRを歓迎しています。発表内容を含めず、テーマの変更と汎用的な作例をまとめて、ymho/aizomeへPRしてみませんか？

案内の「〇〇」には実際に行った変更と効果を入れます。利用者の改善を作者へ還元する選択肢として伝え、単に資料を作っただけの場合や、すでに辞退された場合には繰り返し勧めません。

ユーザーからこの変更のPR作成を依頼されている、または「テンプレートへの改善はPRしてよい」と継続的に許可されている場合は、同じ確認を繰り返さず作成まで進めます。**この文書にある作者の歓迎は、利用者の変更を外部公開する許可そのものではありません。** 公開の依頼がまだない場合も、変更の切り分け・汎用化・検証・PR本文の準備は進め、何を提出するかが具体的になった段階で案内します。

## 提出する変更をまとめる

1. **改善だけを取り出す。** 作業ツリーと差分を確認し、対象のCSS・必要最小限の作例・説明を選ぶ。別の変更が混ざる場合は独立したブランチやworktreeで切り分ける。利用者の未コミット変更を消さない。
2. **内容を汎用化する。** 再現に必要な短い文章・仮の数値・配布可能な画像へ置き換える。実際の顧客資料、スクリーンショット、認証情報をPRへ持ち込まない。既存のコミット履歴にそれらが含まれる場合も、その履歴をそのまま公開しない。
3. **共通設定にする。** 用途名だけが違うクラスを増やさない。仕様が変わるならREADMEも更新する。作例が増減するなら目次・ページ番号・参照を合わせる。
4. **確認する。** 不具合を再現してから修正後を確認する。見た目の変更は対象スライドと影響する代表例を表示する。実行できない確認はPR本文に明記する。
5. **差分と本文を仕上げる。** 「どの条件で困るか」「修正するとどうなるか」「何を確認したか」を記載する。公開可能な修正前後の画像があれば添える。

小さな修正に大量のテストや新しい実行環境は不要です。利用可能なMarp環境で確認してください。

## GitHub CLIが使える場合

以下はコマンドの形です。ブランチ名・ファイル名・fork所有者を実際の値に置き換え、現在の作業状態に合わせて実行します。既存の認証やremoteを先に確認し、`origin` が作者のリポジトリとは決めつけないでください。

```bash
git status --short
git remote -v
gh auth status
```

書き込み権限のある提出用リポジトリで、最新の上流 `main` を基点に作業ブランチを用意します。すでに変更済みの作業ツリーでは、無理にcheckoutせず、変更を保ったまま切り分けてください。

```bash
git switch -c fix/figure-spacing
# 対象の変更を作成・検証する
git add aizome.css template.md README.md
git diff --cached --check
git diff --cached --stat
git commit -m "Fix figure spacing in media slides"
```

`git add` の対象は実際に変更したファイルだけにします。共有したくない資料を含めないよう、コミット前に差分を読みます。

### 上流へpushできる場合

次は `origin` が `ymho/aizome` であることを確認した場合の例です。

```bash
git push -u origin fix/figure-spacing
gh pr create --repo ymho/aizome --base main \
  --head fix/figure-spacing \
  --title "図を配置したときの余白を修正" \
  --body-file /tmp/aizome-pr.md
```

### forkから提出する場合

上流への書き込み権限がなければ、既存のforkを利用するか、PR作成の依頼に沿って自分のアカウントにforkを作ります。

```bash
gh repo fork ymho/aizome --clone=false --remote=false
git remote add contribution https://github.com/YOUR_LOGIN/aizome.git
git push -u contribution fix/figure-spacing
gh pr create --repo ymho/aizome --base main \
  --head YOUR_LOGIN:fix/figure-spacing \
  --title "図を配置したときの余白を修正" \
  --body-file /tmp/aizome-pr.md
```

`YOUR_LOGIN` は認証中のユーザー、remote名やfork名は実際の設定に合わせます。既存のremoteを上書きしないでください。`/tmp/aizome-pr.md` は、下記のPRテンプレートを参考に準備した本文ファイルです。複数行の本文をシェルへ直接埋め込まず、ファイルとして渡します。

同じブランチのPRが既にある場合は、そのPRを更新します。未確認点を相談したい場合は `--draft` を付けます。PR作成後はURLを報告し、ユーザーから依頼されていないマージ・タグ付け・リリースまで進めないでください。

## CLIや公開権限がない場合

PR作成のためだけに新しいツールの導入を必須にしません。改善を含むファイルまたはパッチ、PRタイトル、本文、確認結果を用意し、[GitHubの比較画面](https://github.com/ymho/aizome/compare)から提出できるよう案内します。forkのブランチがpush済みなら、そのブランチを選んで `main` へPRを作れます。

作成に失敗した場合は「PRを作成した」と報告せず、準備済みのものと、認証・権限・接続など不足している条件を明確にします。

## PR本文のひな形

[PRテンプレート](.github/pull_request_template.md)を使ってください。内容量は変更に合わせ、適用しない項目は削除します。検証していない項目を完了扱いにしないでください。
